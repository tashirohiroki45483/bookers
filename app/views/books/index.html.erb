<h1>Books</h1>

  <h4>Title</h4>
  <h4>Body</h4>

<% @book2.each do |book| %>
  <span><%= book.title %></span>
  <span><%= book.body %></span>

<% end %>

<h1>new book</h1>

<%= form_with model: @book, url: '/books', method: :post do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>

