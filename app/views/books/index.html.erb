<body>
  <h1>Books</h1>
  
  <table>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
    <% @book2.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= link_to "Show", book_path(book.id) %></td>
      <td><%= link_to "Edit", edit_book_path(book.id) %></td>
      <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></td>
    <% end %>
    </tr>
   </table>
  
<div class = "new_book">
  
  <h1>new book</h1>
  
  <div class = "new_book_erros">
    <% if @book1.errors.any? %>
    <%= @book1.errors.count %>errors prohibited this book from being saved:
    <ul>
      <% @book1.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
    <% end %>
  </div>
  
  <div class = "new_book_toukou">
    <%= form_with model: @book1, url: '/books', method: :post do |f| %>
    <div>Title</div>
    <%= f.text_field :title %>
    <div>Body</div>
    <%= f.text_area :body %>
    <div></div>
    <%= f.submit 'Create Book' %>
    <% end %>
  </div>
</div>

</body>